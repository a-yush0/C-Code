#include <iostream>

using namespace std;

int main() {
    int n;

    cout << "Enter the value of n: ";
    cin >> n;

    cout << "Square numbers up to " << n << " are:\n";

    for (int i = 1; i * i <= n; ++i) {
        cout << i * i << " ";
    }

    return 0;
}
